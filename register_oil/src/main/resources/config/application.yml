spring:
  application:
    name: team-oil-register
  profiles:
    active: dev
  security:
    user:
      name: teamoil
      password: teamoil
server:
  port: 1111
logging:
  file:
    name: logs/register_oil/register_oil.log
